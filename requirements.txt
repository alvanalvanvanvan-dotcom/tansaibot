# Core bot
python-telegram-bot[rate-limiter]==21.6
httpx[http2]==0.27.2
python-dotenv==1.0.1

# Structured logging (#13)
structlog>=24.1.0

# Error tracking (#14)
sentry-sdk>=2.0.0

# Scheduling / reminders (#40)
apscheduler>=3.10.4

# OCR (#31) - optional, requires tesseract binary installed separately
# pytesseract>=0.3.10
# Pillow>=10.3.0

# Webhook mode needs no extra deps (handled by python-telegram-bot)
# Web search uses httpx (already installed)

# Tier L features (#17, #22, #32, #47)
# Admin Dashboard (#17)
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6

# PostgreSQL backend (#22) - optional
# asyncpg>=0.29.0

# RAG & Document processing (#32) - optional
# chromadb>=0.4.0
# sentence-transformers>=2.2.2
# pypdf>=4.0.0
# python-docx>=1.1.0

# System Health monitoring
psutil>=5.9.0
